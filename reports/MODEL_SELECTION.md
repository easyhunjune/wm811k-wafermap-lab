# 최종 테스트 전 모델 선택 기록

- 결정일: 2026-07-28
- 결정 시점: 테스트 평가 전
- 주 지표: validation macro F1
- 보조 지표: balanced accuracy, 클래스별 recall
- 고정 분할 SHA-256: `11fa5f986749f905e6b498b0a40bd4464c0575d6f73fa41215f6269b79f36090`

## 선택

최종 테스트 대상은 **E2**로 고정한다.

| 후보 | validation macro F1 | balanced accuracy | 결정 |
|---|---:|---:|---|
| E1 | 0.8478 | 0.8104 | 미선택 |
| E2 | **0.8496** | 0.8025 | 선택 |
| E3 | 0.8355 | **0.8762** | 미선택 |

사전 정의한 주 지표가 macro F1이므로 E2를 선택한다. 다만 E1과의 차이는 0.0018로 작고 단일 시드 결과이므로 2채널 입력의 우월성을 주장하지 않는다.

E3는 minority recall과 balanced accuracy가 높지만 macro F1이 낮고, 역빈도 가중치 최대/최소 비가 1,038.8배여서 false positive와 학습 변동성이 커졌다. 따라서 E3는 최종 모델이 아니라 recall-precision trade-off를 보여 주는 불균형 대응 실험으로 남긴다.

이 문서를 저장한 뒤 E2의 `best.pt`를 불러와 테스트를 한 번만 평가한다. 테스트 결과를 확인한 뒤 모델이나 하이퍼파라미터를 다시 선택하지 않는다.

