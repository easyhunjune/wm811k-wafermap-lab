# 클래스 가중치 정규화 프로브 (E3)

## 질문

`mean_one`은 9개 클래스 가중치의 산술평균을 1로 맞춘다. 배치 평균 손실을 쓰므로 옵티마이저가 보는 실효 스케일은 표본 기준 가중 평균이 되어 1보다 훨씬 작아진다. `sample_mean_one`은 이 표본 기준 평균을 1로 맞춰 비가중 실험과 손실 스케일을 맞춘다. 두 방식은 상수배 관계라 가중치 **모양**은 같고, 따라서 이 비교는 실효 학습률 차이만을 분리한다.

## 실행 조건

- 실험: E3, 학습 seed: 42, 52, 62, 72, 82
- 분할·모델·손실 공식·optimizer·학습률·나머지 학습 설정 고정
- 검증 데이터만 사용. 테스트 데이터는 사용하지 않았다.
- **코드 버전은 두 arm이 다르다**: baseline 미기록(`git_commit: null`), 프로브 `84bc2bc`. 프로브 arm은 체크포인트 저장 조건에서 `min_delta`를 분리한 뒤의 코드이며, 이 변경은 더 높은 macro F1을 저장하므로 편향 방향이 프로브 arm에 유리하다.

| 정규화 | 실효 손실 스케일 |
|---|---:|
| `mean_one` | 0.0397 |
| `sample_mean_one` | 1.0000 |

## seed별 검증 macro F1

| seed | `mean_one` | `sample_mean_one` | 차이 | best epoch (전) | best epoch (후) |
|---|---:|---:|---:|---:|---:|
| 42 | 0.8355 | 0.7215 | -0.1139 | 23 | 4 |
| 52 | 0.8161 | 0.8124 | -0.0037 | 16 | 16 |
| 62 | 0.8329 | 0.8407 | +0.0078 | 25 | 26 |
| 72 | 0.8227 | 0.7882 | -0.0346 | 15 | 9 |
| 82 | 0.7965 | 0.7846 | -0.0119 | 7 | 7 |

## 판정

- `sample_mean_one` − `mean_one` macro F1: 평균 -0.0313, seed 간 표준편차 0.0487, 상승 1/5 seed. 평균 차이가 seed 간 표준편차를 넘지 않고 방향도 seed마다 갈린다. 일관된 차이로 판정하지 않는다.
- `sample_mean_one` − `mean_one` balanced accuracy: 평균 -0.0512, seed 간 표준편차 0.0784, 상승 1/5 seed. 평균 차이가 seed 간 표준편차를 넘지 않고 방향도 seed마다 갈린다. 일관된 차이로 판정하지 않는다.
- `none` false positive: 평균 235.4건 증가 (± 390.8), 증가 3/5 seed. 방향이 seed마다 갈린다.
- macro F1 seed 간 표준편차: `mean_one` 0.0156 → `sample_mean_one` 0.0441. 실행 간 변동이 2.8배로 커졌다.

### 클래스별 판정

- Scratch: 재현율 -0.1597 (하락 3/5 seed), 정밀도 -0.0119 (하락 2/5 seed). 재현율 상승과 정밀도 하락의 절충 패턴은 확인되지 않는다.
- Loc: 재현율 -0.0807 (하락 2/5 seed), 정밀도 +0.0506 (상승 2/5 seed). 재현율 상승과 정밀도 하락의 절충 패턴은 확인되지 않는다.
- Edge-Loc: 재현율 -0.0830 (하락 3/5 seed), 정밀도 +0.0218 (상승 4/5 seed). 재현율 상승과 정밀도 하락의 절충 패턴은 확인되지 않는다.
- Donut: 재현율 -0.0763 (하락 4/5 seed), 정밀도 +0.0297 (상승 4/5 seed). 재현율 상승과 정밀도 하락의 절충 패턴은 확인되지 않는다.

## 이 프로브가 말하지 않는 것

- E3 외 실험. 실효 스케일 축소 폭은 가중치 공식마다 다르므로 여기서 얻은 결과를 다른 실험에 그대로 적용할 수 없다.
- 테스트 성능. 검증 데이터만 사용했다.
- 학습률을 함께 조정했을 때의 결과. 학습률은 고정하고 손실 스케일만 바꿨다.
